import { OnlyNumbersNotToStarZeroDirective } from './only-numbers-not-to-star-zero.directive';

describe('OnlyNumbersNotToStarZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumbersNotToStarZeroDirective();
    expect(directive).toBeTruthy();
  });
});
