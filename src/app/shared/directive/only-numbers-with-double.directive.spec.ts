import { OnlyNumbersWithDoubleDirective } from './only-numbers-with-double.directive';

describe('OnlyNumbersWithDoubleDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumbersWithDoubleDirective();
    expect(directive).toBeTruthy();
  });
});
