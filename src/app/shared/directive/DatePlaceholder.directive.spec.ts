/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DatePlaceholderDirective } from './DatePlaceholder.directive';

describe('Directive: DatePlaceholder', () => {
  it('should create an instance', () => {
    const directive = new DatePlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
